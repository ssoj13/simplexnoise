# - Config file for '@PROJECT_NAME@' package
# It defines the following variables
#  @PROJECT_NAME_UPPERCASE@_INCLUDE_DIRS - include directories
#  @PROJECT_NAME_UPPERCASE@_LIBRARIES    - libraries to link against

# Include directory
set(@PNAME_UPPERCASE@_INCLUDE_DIRS "@INSTALL_INCLUDE_DIR@")

# Import the exported targets
include("${CMAKE_CURRENT_LIST_DIR}/@PNAME@Targets.cmake")

# Set the expected library variable
set(@PNAME_UPPERCASE@_LIBRARIES @LIBRARY_NAME@)
